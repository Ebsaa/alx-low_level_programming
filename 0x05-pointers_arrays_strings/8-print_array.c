#include <stdio.h>
#include "main.h"

/**
* print_array - prints n elements of an array of int.
* followed by a new line
* @a: array supposed to be printed
* @n: number of elements to print
*/
void print_array(int *a, int n)
{
int b;

for (b = 0; b < n; b++)
{
if (i == 0)
printf("%d", a[b]);
else
printf(", %d", a[b]);
}
printf("\n");
}
